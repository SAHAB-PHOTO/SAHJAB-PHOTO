# Prompt جاهز لإنشاء «نُخبة» في Lovable

> انسخ النص داخل الإطار بالأسفل والصقه في [lovable.dev](https://lovable.dev) كأول رسالة. مكتوب ليُنتج تطبيقاً يعمل بالكامل في المتصفح (React + TypeScript + Tailwind) دون أي خادم.

---

```
ابنِ لي تطبيق ويب احترافياً اسمه «نُخبة» (Nukhba) — محطّة فرز صور (Photo Culling) للمصوّرين، يعمل بالكامل في المتصفح (Client-side فقط، بدون أي backend أو قاعدة بيانات سحابية أو رفع للصور). اللغة عربية والاتجاه RTL.

# التقنيات
- React + TypeScript + Vite + Tailwind CSS + shadcn/ui.
- لا أي خادم: كل المعالجة في المتصفح عبر Canvas API و File API و IndexedDB.
- الصور لا تُرفع أبداً لأي مكان. استخدم URL.createObjectURL للعرض.
- خط: Tajawal للعربية، Space Grotesk للأرقام/اللاتيني.

# الفكرة والقيمة
المصوّر يقضي 80% من وقته في انتقاء الصور لا التصوير. «نُخبة» يفرز آلاف الصور بسرعة الكيبورد، يكتشف الصور غير الواضحة آلياً، ويجمّع اللقطات المتسلسلة المتشابهة — مجاناً، محلياً، بخصوصية تامة، ويعمل دون إنترنت.

# الشاشات
1) شاشة بداية (Landing): شعار + عنوان جذّاب «افرز آلاف الصور في دقائق لا ساعات» + شارة «🔒 يعمل دون إنترنت · صورك لا تغادر جهازك» + ثلاثة أزرار: «اختر صوراً»، «افتح مجلّداً كاملاً» (input webkitdirectory)، «جرّب بصور تجريبية». + شبكة 4 مزايا.
2) شاشة العمل (Studio): شريط علوي فيه (إحصاءات: الإجمالي/مقبولة/مرفوضة/غير واضحة/مجموعات) + فلاتر + زر اختصارات + قائمة تصدير منسدلة. وسطها عارض كبير للصورة الحالية مع مؤشّر حدّة وشارات حالة وسهمَي تنقّل. تحته شريط أدوات حكم (رفض ✕ / نجوم 1–5 / قبول ✓ / تكبير 100% / قارن). أسفله شريط مصغّرات أفقي بشارات.

# المزايا التفصيلية
- استيراد صور متعددة أو مجلّد كامل، مع سحب وإفلات على كامل النافذة.
- فرز بالكيبورد: ←→ تنقّل، P قبول، X رفض، 1–5 نجوم، 0 إزالة، Z أو مسافة تكبير 100%، G القفز لأوضح صورة في المجموعة، C فتح المقارنة، Esc إغلاق النوافذ. (ملاحظة RTL: السهم الأيمن = الصورة السابقة بصرياً). بعد القبول/الرفض ينتقل تلقائياً للتالية.
- شريط مصغّرات: كل مصغّرة تعرض شارة قبول/رفض ونجوم وتحذير «غير واضحة»، وعلامة المجموعة، والمصغّرة المرفوضة تظهر باهتة. المصغّرة النشطة مميّزة وتُمرّر للوسط تلقائياً.
- فلاتر: الكل / غير مفروزة / مقبولة / مرفوضة / ★3+ / غير واضحة.
- وضع المقارنة (C): نافذة تعرض لقطات المجموعة الحالية جنباً إلى جنب (أو الصور المتجاورة إن لم توجد مجموعة)، كل واحدة بمؤشّر حدّتها واسمها، وتبرز «الأوضح» تلقائياً. زر «اختر هذه» يقبل المختارة ويرفض بقية المجموعة دفعة واحدة.

# الخوارزميات (نفّذها فعلياً، لا تزييف)
1) كشف الحدّة (Sharpness): ارسم الصورة على Canvas بعرض 256px، حوّلها لرمادي، طبّق مرشّح Laplacian (kernel: مركز -4 وجيرانه الأربعة +1)، احسب تباين النتيجة (variance)، ثم حوّله إلى مقياس 0–100 لوغاريتمياً: score = min(100, (log10(variance+1)/log10(2500))*100). الصور التي حدّتها < 35 تُعلَّم «قد تكون غير واضحة».
2) تجميع اللقطات المتسلسلة (Burst grouping): احسب بصمة إدراكية aHash لكل صورة (صغّرها إلى 8×8 رمادي، احسب المتوسط، كل بكسل ≥ المتوسط = 1 وإلا 0 → 64 بت كـ BigInt). صورتان من نفس المجموعة إذا كانت مسافة هامِنغ بينهما ≤ 8. أعطِ كل مجموعة معرّفاً، وأفضل صورة فيها = الأعلى حدّةً. نفّذ التحليل في الخلفية بالتتابع دون تجميد الواجهة، وأظهر تأثير «shimmer» على المصغّرة قيد التحليل.
3) دعم RAW: اقبل امتدادات CR2/CR3/NEF/ARW/RAF/ORF/RW2/DNG/PEF/SRW/NRW/SR2/RWL. لفكّها اقرأ ArrayBuffer وامسح البايتات لاستخراج أكبر JPEG مضمّن (ابحث عن FF D8 FF بداية وأقرب FF D9 نهاية، واختر الأطول)، اعرض هذه المعاينة. احتفظ بالملف الأصلي للتصدير. تجاهل بأناقة الملفات التي تفشل.

# حفظ الجلسة (IndexedDB)
احفظ تلقائياً لكل صورة: التقييم، العلامة (قبول/رفض)، الحدّة، البصمة — بمفتاح «بصمة الملف» = `${name}__${size}__${lastModified}`. عند إعادة فتح نفس الملفات لاحقاً استعد القرارات والتحليل فوراً وتخطَّ إعادة التحليل، وأظهر رسالة «استُعيدت قرارات N صورة». أضف زراً «مسح ذاكرة الجلسة».

# التصدير (نفّذ مولّد ZIP بلغة JS خالصة بطريقة Store + CRC32، دون مكتبات)
قائمة منسدلة بثلاثة خيارات:
1) قائمة أسماء .txt للصور المختارة (قبول أو ★3+) للّصق في فلتر Lightroom/Capture One.
2) ملفات XMP بالتقييمات داخل .zip: لكل صورة محكومة ملف baseName.xmp يحوي <xmp:Rating> و<xmp:Label> (Select للمقبول، Reject للمرفوض) بصيغة xpacket/rdf قياسية تقرأها Lightroom وBridge.
3) الصور الأصلية المقبولة نفسها مجمّعة في .zip واحد، مع عرض الحجم بالميغابايت.

# التصميم (Dark، احترافي، سينمائي)
- خلفية داكنة جداً (#070810)، بطاقات (#0e1019 / #141724)، حدود رقيقة بيضاء شفافة.
- ألوان مميّزة: سماوي #7ee8fa ودافئ #feb47b كتدرّج رئيسي؛ أخضر #5fe39a للقبول، أحمر #ff6b6b للرفض، ذهبي #ffd166 للنجوم.
- زجاجية (glassmorphism) للشارات فوق الصورة، خلفية شفقية متحركة (blobs) في شاشة البداية، حواف دائرية، ظلال ناعمة، انتقالات لطيفة.
- مؤشّر الحدّة شريط بتدرّج أحمر→ذهبي→أخضر. RTL كامل وتنسيق عربي للأرقام والتواريخ.

# صور تجريبية
زر يولّد 15 صورة عبر Canvas: 5 «مشاهد» × 3 لقطات متقاربة لكل مشهد (تزحزح بسيط للموضوع لمحاكاة اللقطة المتسلسلة)، واجعل اللقطة الوسطى في كل مشهد مموّهة (filter blur) لتجربة كشف الحدّة والتجميع فوراً.

اجعل الكود نظيفاً ومقسّماً لمكوّنات، والأداء عالياً مع مئات الصور (lazy loading للمصغّرات، وفكّ الصورة كاملة الدقة للصورة النشطة فقط).
```

---

## ملاحظات للاستخدام في Lovable

- **الصقه كأول رسالة** كاملاً؛ Lovable يبني هيكلاً أوّلياً ثم تطلب تحسينات.
- **رسائل متابعة مفيدة** بعد البناء الأول:
  - «حسّن لوحة المفاتيح بحيث لا تتعارض مع حقول الإدخال، وأضف مؤشّراً بصرياً للاختصار النشط.»
  - «تأكّد أن مولّد ZIP ينتج أرشيفاً صالحاً (طريقة Store + CRC32) واختبره بصورتين.»
  - «أضف وضع المقارنة بالتكبير المتزامن (panning مزامن بين الصورتين).»
- إن أردت ربط نطاق أو نشر: استخدم زر **Publish** في Lovable (الاستضافة لديهم، والتطبيق client-side فيعمل مباشرة).
- الكود المرجعي الكامل لـ«نُخبة» موجود في هذا المستودع (`index.html` / `styles.css` / `app.js`) إن رغبت بمطابقة سلوك أي ميزة بدقّة.
